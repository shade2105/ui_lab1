<Window x:Class="lab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab1"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600" MinHeight="200" MinWidth="520" Background="#BFBCCB">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="3" Grid.Row="0" Height="15" Background="#BFBCCB"/>
        <Border Grid.ColumnSpan="3" Grid.Row="3" Height="15" Background="#BFBCCB"/>

        <Border Grid.Column="1" Grid.Row="1" BorderBrush="Black"  Background="#F4F3F6">
            <Grid Margin="10">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Name="AnswerRow" Height="40"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="Question_Name" HorizontalAlignment="Center" Text="#Question_Name" FontSize="32" Margin="10,0,10,10" Grid.Row="0"/>
                <TextBlock x:Name="Question_Text" HorizontalAlignment="Center" Text="#Question_Description" FontSize="16" Margin="10,0,10,10" Grid.Row="1"/>
                <TextBox x:Name="Answer"  Background="#F4F3F6" Margin="100,10,100,10" Text="#Answer" FontSize="14" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextWrapping="NoWrap" AcceptsReturn="False"/>
                
                <Button x:Name="Submit"  IsEnabled="{Binding Text.Length, ElementName=Answer}" Content="#Submit" Click="Button_Click" Grid.Row="3" HorizontalAlignment="Center" Margin="0,10,0,00" Height="30" Width="90"/>

            </Grid>

        </Border>

    </Grid>
</Window>
